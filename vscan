#!/usr/bin/env python3
"""
Convenience wrapper to run vscan from development directory.

This script allows you to run the package directly without installation:
    ./vscan --help
    ./vscan scan --output test.json

This is equivalent to:
    python -m vscode_scanner.vscan --help
"""
if __name__ == "__main__":
    from vscode_scanner.vscan import cli_main

    cli_main()
